_min_copier_version: "9.0.0"
_subdirectory: template

# ── Questions ──────────────────────────────────────────────────
project_name:
  default: Webapp
author_name:
  default: ""
description:
  default: ""
author_email:
  default: ""
repository_url:
  default: ""
data_dir:
  default: "data"
data_directory_name:
  default: "DATASCI"
llm_endpoint:
  default: ""

# ── Tasks and other config (NO UNDERSCORE ON THESE) ─────────────
tasks:
  - name: pull-in-common
    bash: echo "Youre in a task!"
    # copier:
    #   repo_url: "{{ _copier_src_path }}/../common"
    #   src_path: template
    #   dst_path: "."
    #   overwrite: true
    #   answers:
    #     project_name: "{{ project_name }}"
    #     author_name: "{{ author_name }}"
    #     description: "{{ description }}"
    #     author_email: "{{ author_email }}"
    #     repository_url: "{{ repository_url }}"
    #     data_dir: "{{ data_dir }}"
    #     data_directory_name: "{{ data_directory_name }}"
    #     llm_endpoint: "{{ llm_endpoint }}"

  - name: run-hook
    bash: ./hooks/post_gen_init_llm_utils.sh

skip_if_exists:
  - README.md